{
  "name": "vinque-main",
  "version": "1.0.0",
  "description": "Vinque application with frontend and backend",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "concurrently \"cd Backend && npm run start\" \"cd pro && npm run dev\"",
    "dev": "concurrently \"cd Backend && npm run dev\" \"cd pro && npm run dev\"",
    "install-all": "npm install && cd Backend && npm install && cd ../pro && npm install"
  },
  "dependencies": {
    "axios": "^1.9.0",
    "concurrently": "^8.2.2",
    "react-icons": "^5.5.0",
    "react-router-dom": "^7.6.0",
    "swiper": "^11.2.6"
  }
}
